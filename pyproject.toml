[project]
name = "tecniber-app"
version = "0.1.0"
description = "Tecniber automatitzation"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pypdf>=6.7.2",
    "pytest>=9.0.2",
    "reportlab>=4.4.10",
    "streamlit>=1.54.0",
]

[tool.pytest.ini_options]
# pythonpath = ["src"]
